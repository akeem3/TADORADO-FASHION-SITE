import type { Metadata } from "next";
import "./globals.css";
import Navbar from "./Components/Nav/Navbar";
import Footer from "./Components/Nav/Footer";
import { Poppins } from "next/font/google";
import { CartProvider } from "@/components/ui/CartContext";

export const metadata: Metadata = {
  title: "Tadorado Fashion",
  description: "Generated by create next app",
  icons: {
    icon: [
      { url: "/images/top-logo.png", type: "image/png" }, // ✅ Top logo (main favicon)
      { url: "/images/top-logo.png", sizes: "32x32", type: "image/png" },
      { url: "/images/top-logo.png", sizes: "16x16", type: "image/png" },
    ],
    apple: "/images/top-logo.png", // ✅ Apple touch icon
  },
};

const poppins = Poppins({ subsets: ["latin"], weight: ["400", "700"] });

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${poppins.className} text-slate-700`}>
        <CartProvider>
          <div className="flex flex-col min-h-screen">
            <Navbar />
            <main className="flex-grow">{children}</main>
            <Footer />
          </div>
        </CartProvider>
      </body>
    </html>
  );
}
