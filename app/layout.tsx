import type { Metadata } from "next";
import "./globals.css";
import Navbar from "./Components/Nav/Navbar";
import Footer from "./Components/Nav/Footer";
import { Poppins } from "next/font/google";
import { CartProvider } from "@/components/ui/CartContext";

export const metadata: Metadata = {
  title: "Tadorado Fashion",
  description: "Generated by create next app",
  icons: {
    icon: [
      { url: "/icons/tado-favicon.svg", type: "image/svg+xml" },
      { url: "/images/Tado logo Final.png", sizes: "32x32", type: "image/png" },
      { url: "/images/Tado logo Final.png", sizes: "16x16", type: "image/png" },
    ],
    apple: "/images/Tado logo Final.png",
  },
};

const poppins = Poppins({ subsets: ["latin"], weight: ["400", "700"] });

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${poppins.className} text-slate-700`}>
        <CartProvider>
          <div className="flex flex-col min-h-screen">
            <Navbar />
            <main className="flex-grow">{children}</main>
            <Footer />
          </div>
        </CartProvider>
      </body>
    </html>
  );
}
